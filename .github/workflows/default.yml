name: Articated CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: setup
      run: apt-get install qt5-dev
    - name: configure
      run: cmake .
    - name: make
      run: make
    - name: make check
      run: make check
